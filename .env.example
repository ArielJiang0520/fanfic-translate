# Local SQLite file. Production sets this to /data/app.db via fly.toml.
DB_PATH=./local.db

# OpenRouter key for the translation call (https://openrouter.ai/keys). Required — without it
# /api/translate returns 500. On Fly: `fly secrets set OPENROUTER_API_KEY=...`
OPENROUTER_API_KEY=
